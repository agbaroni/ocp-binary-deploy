apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{ .Values.application.name | quote }}
  name: dbms_environment
  namespace: {{ .Values.project.name | quote }}
data:
  MYSQL_USER: test
  MYSQL_PASSWORD: Test123!
  MYSQL_DATABASE: ocpbb
immutable: true
